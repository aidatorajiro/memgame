#:kivy 1.0.9
#:import ew kivy.uix.effectwidget

<Character>:
    size: 0, 0

<Footprints>:
    size: 0, 0

<ProcessSelect>:
    size: 0, 0

<MemGame>:
    chara: character
    select: process_select
    footprints: footprints
    
    Character:
        id: character
        center: self.parent.center
    
    ProcessSelect:
        id: process_select
        center: self.parent.center
    
    Footprints:
        id: footprints
        center: self.parent.center